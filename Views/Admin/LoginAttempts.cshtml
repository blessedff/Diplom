@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Логи попыток входа";
}

<h2 class="mb-4"><i class="bi bi-shield-lock"></i> Логи попыток входа</h2>

<div class="alert alert-info">
    <i class="bi bi-info-circle"></i> Здесь будут логи входов (добавим позже)
</div>